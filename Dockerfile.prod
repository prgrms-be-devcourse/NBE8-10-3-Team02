# CI에서 이미 빌드된 JAR을 사용하는 경량 이미지
FROM eclipse-temurin:21-jre

WORKDIR /app

# 빌드된 JAR 복사
COPY build/libs/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java", "-XX:MaxRAMPercentage=75.0", "-XX:+ExitOnOutOfMemoryError", "-XX:+UseG1GC", "-jar", "app.jar", "--spring.profiles.active=prod"]
